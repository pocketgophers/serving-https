Example code for https://pocketgophers.com/serving-https/

Get the code with:

```
go get -d pocketgophers.com/serving-https
```

Run the examples with:

```
go run example.go
```

replacing `example.go` with the example you wish to run